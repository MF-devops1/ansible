# - name: PING
#   hosts: web
#   tasks: # task=module=collection
#   - name: ping the server
#     ansible.builtin.ping:


- name: ping
  hosts:  web 
  tasks: 
    - name: ping the server 
      ansible.builtin.ping:

